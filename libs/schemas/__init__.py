"""Shared data contracts (CPO, JPO, etc.) between all Meridian services.

Real schema definitions land in Phase 1. This placeholder exists so the
CI pipeline (lint/type-check/test) has real code to run against from the
very first commit, rather than sitting green on an empty repo.
"""

__version__ = "0.0.1"
