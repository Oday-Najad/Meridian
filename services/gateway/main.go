package main

import "fmt"

// main is a placeholder entrypoint so `go build`/`go vet` have something real
// to check from commit one. Real HTTP routing, auth middleware, and service
// coordination land when we start Phase 2 (matching-engine) and need a real
// endpoint to route to.
func main() {
	fmt.Println("meridian-gateway: placeholder, not yet serving")
}
