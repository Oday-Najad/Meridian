# application-agent

Stateful workflow (submit -> await approval -> send -> watch for reply ->
draft response -> await approval) with a human-approval interrupt as a graph
node, not a UI afterthought.

Status: not yet implemented — designed in Phase 6.
