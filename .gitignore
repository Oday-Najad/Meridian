# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
htmlcov/
.coverage

# Env / secrets
.env
.env.*
!.env.example

# OS / IDE
.DS_Store
.idea/
.vscode/

# Docker
*.log

# Model artifacts (large files — will use proper artifact storage later, not git)
*.gguf
*.safetensors
*.bin
checkpoints/

# Node / Next.js
node_modules/
.next/
out/
apps/web/.env*.local

# Go
services/gateway/meridian-gateway
services/gateway/*.exe

# Supabase local dev
.supabase/

# NOTE: generated schema files (libs/schemas-json/, libs/schemas-ts/,
# services/gateway/internal/schemas/generated.go) are intentionally NOT
# ignored -- they must be committed so CI's schema-drift-check can diff
# against them.
