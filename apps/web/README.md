# web

Next.js + TypeScript frontend. Candidate dashboard, recruiter search UI.
Deployed to Vercel. Talks to the Go gateway over REST/JSON, auth handled via
Supabase Auth (client SDK + JWT passed to the gateway).

Status: not yet implemented — scaffolded in the polyglot-stack revision
(ADR-0002). Real screens land once the matching-engine (Phase 2) has
something real to display.
